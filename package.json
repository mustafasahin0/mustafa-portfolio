{
  "name": "astrofy",
  "description": "Astrofy is a free and open-source template for your Personal Portfolio Website built with Astro and TailwindCSS. Create in minutes a website with Blog, CV, Project Section, Store and RSS Feed.",
  "type": "module",
  "version": "3.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "kill": "pkill -9 node; pkill -f cloudflared;",
    "clean": "rm -rf dist .astro node_modules/.vite",
    "live": "npm run build && npm run preview & sleep 30 && cloudflared tunnel --no-autoupdate run --url http://localhost:4321 pixel8"
  },
  "dependencies": {
    "@astrojs/mdx": "^2.0.3",
    "@astrojs/node": "^8.3.3",
    "@astrojs/tailwind": "^5.0.3",
    "astro": "^4.0.2",
    "daisyui": "^4.4.10",
    "dayjs": "^1.11.9",
    "tailwindcss": "^3.3.5"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.10"
  }
}
